export type ToDoItem = {
  id: string;
  text: string;
  deadline: string;
  isDone: boolean;
  title: string;
};

export type NewToDoItem = {
  text: string;
  deadline: string;
  isDone: boolean;
};
